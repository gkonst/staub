<!DOCTYPE html
        PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN"
        "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:h="http://java.sun.com/jsf/html"
      xmlns:f="http://java.sun.com/jsf/core"
      xmlns:a4j="http://richfaces.org/a4j"
      xmlns:ui="http://java.sun.com/jsf/facelets"
      xmlns:s="http://jboss.com/products/seam/taglib">
<ui:composition template="../template/staub-template.jspx">
    <ui:define name="pageScript">
        <a4j:loadScript src="../script/timer.js"/>
    </ui:define>
    <ui:define name="sidebar">
        <a4j:outputPanel id="timerPanel"
                         rendered="#{questionBean.hasNextQuestion and questionBean.currentQuestion.timeLimitPresent}">
            Time - <span id="timerField"/>
            <script type="text/javascript">
                Timer.init({timeToCount : '#{questionBean.currentQuestion.timeLimit}',
                    currentTime : '#{questionBean.currentTime}',
                    timerFieldId : 'timerField'});
            </script>
            <br/>
            #{questionBean.currentQuestion.timeLimit}
            <br/>
            #{questionBean.currentTime}
        </a4j:outputPanel>

        <h:form>
            <a4j:poll action="#{questionBean.refreshTimer}" reRender="timerPanel" interval="10000"
                      enabled="#{questionBean.hasNextQuestion and questionBean.currentQuestion.timeLimitPresent}"/>
        </h:form>
    </ui:define>
    <ui:define name="contentTitle">
        #{questionBean.currentQuestion.name}
    </ui:define>
    <ui:define name="contentMain">
        <a4j:outputPanel id="questionContentPanel" rendered="#{questionBean.hasNextQuestion}">
            <ui:include src="../controls/questionDefinitionControl.jspx"/>
            <ui:include src="../controls/questionAnswerControl.jspx"/>
        </a4j:outputPanel>
        <a4j:outputPanel id="noQuestionPanel" rendered="#{!questionBean.hasNextQuestion}">
            Вопросы кончились!
            Тест завершен!
        </a4j:outputPanel>
    </ui:define>
    <ui:define name="contentButtons">
        <a4j:commandButton id="prevQuestionButton"
                           action="#{questionBean.previousQuestion}"
                           value="#{messages['question.detail.previousQuestion']}"
                           styleClass="button"
                           disabled="#{!questionBean.hasPreviousQuestion}"
                           reRender="content, sidebar"
                           rendered="false"/>
        <a4j:commandButton id="nextQuestionButton"
                           action="#{questionBean.nextQuestion}"
                           value="#{messages['question.detail.nextQuestion']}"
                           styleClass="button"
                           rendered="#{questionBean.hasNextQuestion}"
                           reRender="content, sidebar"
                           onclick="Timer.destroy()"/>
        <a4j:jsFunction name="nextQuestion"
                        action="#{questionBean.nextQuestion}"
                        reRender="content, sidebar"/>
        <s:button id="endTestButton"
                  action="#{questionBean.endTest}"
                  value="#{messages['question.detail.endTest']}"
                  styleClass="button"
                  rendered="#{!questionBean.hasNextQuestion}"
                  propagation="end"/>
    </ui:define>
</ui:composition>
</html>